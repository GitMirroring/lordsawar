Copyright and license notices are held in the comments section of each image file in this directory.

Use the `identify -verbose' command from imagemagick to read the comments in the image files.

These images were created specifically for lordsawar, except for button_move.png which before it was modified comes from move_panel_object_pointer.png, of an old GNOME user guide.
